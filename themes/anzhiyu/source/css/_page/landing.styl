# Landing page styles

#landing
  position: relative
  width: 100%
  min-height: 100vh
  overflow: hidden
  background: radial-gradient(1200px at 50% 30%, rgba(50,60,90,.35), transparent 70%),
              radial-gradient(800px at 80% 80%, rgba(40,50,80,.25), transparent 60%),
              var(--anzhiyu-background)

#landing-canvas
  position: absolute
  inset: 0
  width: 100%
  height: 100%
  display: block
  z-index: 0

.landing-content
  position: relative
  z-index: 1
  min-height: 100vh
  display: flex
  flex-direction: column
  align-items: center
  justify-content: center
  text-align: center
  padding: 4rem 1.5rem

.site-title
  font-size: clamp(2.2rem, 5vw, 4.5rem)
  font-weight: 800
  letter-spacing: .02em
  margin: 0 0 .6rem
  color: var(--anzhiyu-fontcolor)

#subtitle
  font-size: clamp(1.1rem, 2.2vw, 1.6rem)
  line-height: 1.6
  color: var(--anzhiyu-secondtext)
  max-width: 880px
  margin: .8rem auto 0
  opacity: .9

.cta-group
  display: flex
  align-items: center
  gap: 1rem
  margin-top: 2.2rem

.cta-button
  display: inline-flex
  align-items: center
  justify-content: center
  padding: .9rem 1.6rem
  font-size: 1rem
  border-radius: 999px
  background: var(--anzhiyu-main)
  color: #fff
  box-shadow: 0 6px 18px rgba(66,90,239,.25)
  transition: transform .25s ease, box-shadow .25s ease
  user-select: none
  -webkit-user-select: none

.cta-button:hover
  transform: translateY(-2px)
  box-shadow: 0 12px 26px rgba(66,90,239,.35)

.audio-toggle
  display: inline-flex
  align-items: center
  justify-content: center
  width: 44px
  height: 44px
  border-radius: 50%
  background: rgba(255,255,255,.08)
  color: var(--anzhiyu-fontcolor)
  backdrop-filter: blur(6px)
  transition: background .2s ease, transform .2s ease

.audio-toggle:hover
  background: rgba(255,255,255,.16)
  transform: translateY(-2px)

@media (max-width: 768px)
  .landing-content
    padding: 3rem 1rem
  .cta-group
    gap: .8rem